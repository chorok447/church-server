spring:
  datasource:
    url: jdbc:mysql://localhost:3306/church_db
    username: root
    password: root
  jpa:
    hibernate:
      ddl-auto: create-drop
    show-sql: true
    defer-datasource-initialization: true
  sql:
    init:
      mode: always
server:
  port: 4000
logging:
  level:
    org:
      springframework:
        security: DEBUG